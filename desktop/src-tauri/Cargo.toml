[package]
name = "friday_desktop"
version = "0.1.0"
description = "FRIDAY AI Desktop Assistant"
authors = ["FRIDAY Team"]
license = ""
repository = ""
default-run = "friday_desktop"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0", features = [] }

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
tauri = { version = "2.0", features = [] }

[features]
default = []
